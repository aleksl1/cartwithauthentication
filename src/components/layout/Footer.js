import classes from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={classes.footer}>
      <span>&copy; A. Sl. - portfolio</span>
    </footer>
  );
};

export default Footer;
